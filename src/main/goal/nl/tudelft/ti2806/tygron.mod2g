use dummy as knowledge.
use tygron as actionspec.
use somegoal as goals.

exit=nogoals.

module helloWorld {
	% find a function to build a road (always 1 floor)
	if goal(havebuilding), bel(functions(FS), member([Name,Id,L],FS), Name == terrace) 
		then building_plan_construction(Id, 1, square(50,50,100,100)).
		
	if goal(have2buildings), bel(functions(FS), member([Name,Id,L],FS), Name == terrace)
		then building_plan_construction(Id, 1, square(60,155,30,30)).

}
