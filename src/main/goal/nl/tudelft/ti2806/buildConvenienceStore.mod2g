use serviceKnowledge as knowledge.
%use serviceBeliefs as beliefs.
%use serviceGoals as goals.
use service as actionspec.

exit = always.

module buildConvenienceStore{
	%Receives the function id from the function list using a member/2 call where the name is 'convenience store'.
	if bel(functions(FS), member([Name,Id,L],FS), (Name == 'convenience store'; Name ='buurtwinkel')  ) 
		then building_plan_construction(Id, 1, square(10,220,30,15)).
}